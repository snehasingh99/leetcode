class Solution:
    def numJewelsInStones(self, J: str, S: str) -> int:
        s = list(J)
        t = list(S)
        count = 0 
        for i in t:
            if(i in s):
                count += 1
            
        return count
